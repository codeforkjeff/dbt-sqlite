
name: dbt-sqlite

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build docker image
        run: docker build . --tag dbt-sqlite:$(date +%s)
      - name: Run tests
        run: docker run dbt-sqlite run_tests.sh
